services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./app:/app
      - ./app/data:/app/data
      - ./app/templates:/app/templates:ro
    restart: unless-stopped